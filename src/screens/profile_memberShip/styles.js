import {StyleSheet} from 'react-native';
export const styles = StyleSheet.create({
  card: {
    borderWidth: 1,
    padding: 10,
    borderColor: 'lightgray',
  },
  nomemship: {
    alignSelf: 'center',
    marginTop: '40%',
  },
});
